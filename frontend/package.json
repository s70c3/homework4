{
  "name": "my-frontend",
  "version": "1.0.0",
  "description": "Frontend for API-based app",
  "scripts": {
    "start": "parcel ./pages/*.html",
    "build": "parcel build ./pages/*.html --no-source-maps --public-url ./",
    "deploy:vercel": "rm -rf dist && npm run build && vercel --prod"
  },
  "keywords": [
    "frontend",
    "static-site"
  ],
  "author": "Your Name",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.12.0",
    "vercel": "^29.0.0"
  }
}
